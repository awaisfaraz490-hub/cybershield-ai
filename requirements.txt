fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic[email]==2.10.3
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
itsdangerous==2.2.0
python-multipart==0.0.19
jinja2==3.1.4

# Optional OCR support (screenshot scanner). The app works without these
# installed — the screenshot scanner will gracefully report OCR as
# unavailable and ask the user to paste text manually instead.
pytesseract==0.3.13
Pillow==11.3.0

# Testing
pytest==8.3.4
httpx==0.28.1
